services:
  postgres:
    image: postgres:18-alpine
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - chongkran_postgres_data:/var/lib/postgresql

volumes:
  chongkran_postgres_data:
